{
    "name": "parent-package",
    "version": "1.0.0",
    "description": "",
    "license": "MIT",
    "scripts": {
        "prestart": "npx kill-port 2000 1234",
        "start": "concurrently \"npm start --prefix server\" \"npm run dev --prefix client\"",
        "start:ports": "npx ts-node scripts/start_ports.ts",
        "test:e2e": "npx ts-node scripts/run_tests.ts",
        "generate-sprites": "npx ts-node scripts/generate_sprite_manifest.ts",
        "install-all": "npm install && concurrently \"npm install --prefix ./server\" \"npm install --prefix ./client\"",
        "generate-docs": "concurrently \"cd ./server && npx typedoc --name openciv/server\" \"cd ./client && npx typedoc --name openciv/client\"",
        "format": "npx prettier --write \"**/*.{ts,mts}\""
    },
    "dependencies": {
        "concurrently": "^7.6.0"
    },
    "devDependencies": {
        "@types/kill-port": "^2.0.3",
        "@types/node": "^24.10.2",
        "kill-port": "^2.0.1",
        "ts-node": "^10.9.2",
        "typedoc": "^0.28.20",
        "typescript": "^5.9.3"
    }
}
